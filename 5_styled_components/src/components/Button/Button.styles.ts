import styled from '@emotion/styled'

export const Button = styled.button`
    background-color: #f1356d;
    border: none;
    padding: 8px;
    border-radius: 8px;
    color: white;
    font-weight: bold;
    cursor: pointer;
    &:hover {
        background-color: #f1356d;
    }
`